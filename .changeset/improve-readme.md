---
---

**📝 Docs**

- Rewrote `README.md`: fixed broken CI badge (was pointing to the deleted
  `cd.yml`), removed dead Docusaurus tabs syntax that didn't render anywhere,
  added Features / Requirements sections, improved the Extending example to
  show how to preserve our `overrides` while appending custom ones. No package
  consumer behavior affected.
